#include<stdio.h>

int main(){
    char str[10];
    int c = 0;
    printf("Enter the string:");
    scanf("%s",&str);
    for(int i = 0;i < str[i]; i++){
        c++;
    }
    printf("The words in string is %d",c);
    return 0;
}